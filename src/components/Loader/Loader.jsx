import { RotateLoader } from "react-spinners";
import css from "./Loader.module.css";
RotateLoader;

const Loader = () => {
  return <RotateLoader color="#ff0000" margin={50} size={40} />;
};

export default Loader;
